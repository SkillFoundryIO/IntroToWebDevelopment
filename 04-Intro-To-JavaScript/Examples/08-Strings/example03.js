let greeting = "Hello, world!";

// returns 0
console.log(greeting.indexOf("H"));

// finds the l in world, index 10
console.log(greeting.indexOf("l", 4));

// returns 1, the starting point of the match
console.log(greeting.indexOf("ello"));